# Lead Magnet PDF — Where to Drop It

`index.html` is already wired to auto-download this file the instant someone
submits the "Get the Free Guide" form. Just add the file here with the
**exact filename** below — no code changes needed.

| File to add (exact name) | What it is |
|---|---|
| `reclaim-focus-guide.pdf` | "Reclaim Focus: Smart Device Rules for Academic Growth" guide |

## Steps
1. Export/save your guide as a PDF named exactly `reclaim-focus-guide.pdf`
   (lowercase, hyphens, no spaces).
2. Copy it into this folder:
   `E:\GTS Ai - Claude Work\claude-workshop\assets\guides\`
3. Push to GitHub (or ask Claude to) so the live site picks it up.
4. Test: submit the "Get the Free Guide" form on the live site — the PDF
   should download automatically.

## Until the file is added
The form still submits and saves the lead to your Google Sheet — the
download link just won't have a file to serve yet, so nothing downloads.
Add the PDF as soon as it's ready and it'll start working immediately.
